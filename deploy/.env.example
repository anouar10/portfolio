# Copy to .env and fill in.  Never commit .env.

# Your domain, without protocol or www.
DOMAIN=anwar.dev

# Where Let's Encrypt sends certificate-expiry warnings.
ACME_EMAIL=salim.anouar10@gmail.com

# Postgres password for the analytics database.
#   openssl rand -base64 24
DB_PASSWORD=change-me

# Umami session secret.
#   openssl rand -base64 32
APP_SECRET=change-me-too
